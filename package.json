{
  "name": "ace-wing-online",
  "version": "1.0.0",
  "private": true,
  "description": "3D dogfight with online modes on Cloudflare Workers",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "build": "echo 'No build needed for static files'",
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "wrangler pages deploy ."
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.86.1"
  }
}
